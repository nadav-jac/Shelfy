# Port the server listens on
PORT=43127

# Base URL embedded in QR codes (no trailing slash).
# Set this to your machine's local network address so phones can scan QR codes.
# Example: VITE_PUBLIC_BASE_URL=http://192.168.1.42:43127
VITE_PUBLIC_BASE_URL=
